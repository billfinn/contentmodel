<div class="well">
    View Taxonomy for&nbsp;
    <% @sites = ::Site.order(:name) %>
    <% @sites.each do |site|  %>
      <%= link_to site.name, taxonomysite_path(site) %>&nbsp;&nbsp;
    <% end %>
</div>
