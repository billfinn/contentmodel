

  <div class="row">
		<div class="col-xs-4">
      <p>Client > Site
      <p>User Flow > Template
      <p>Breadcrumbs > Information Architecture path
        <hr>
			<h3>Messaging</h3>
			<p>Messaging direction for this page, at the given level within the IA.</p>
        <hr>
      <h3>Tags</h3>
      <h3>SEO Terms</h3>
      <h3>Desired Actions</h3>
		</div>
		<div class="col-xs-8">
			<h1>Workflows</h1>

      <% @workflows = Workflow.order(:name) %>
      <% @workflows.each do |workflow|  %>
      <h2><%= workflow.name %></h2></h5>
      <p>
        <%= safe_join(workflow.pages.map { |d| link_to(d.name, page_path(d)) }, "&nbsp;&nbsp;>&nbsp;&nbsp;".html_safe) %>
      </p>
      <% end %>


		</div>
	</div>
  	<hr>
  <div class="row">
    <div class="col-xs-12">
      <footer>
        <p>Content Model v.0.1</p>
      </footer>
    </div>
  </div>
