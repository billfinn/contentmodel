

  <div class="row">
		<div class="col-xs-4">

      <%= render "siteMap" %>

			<h3>Notes</h3>

		</div>
		<div class="col-xs-8">

      <% @site = Site.find(params[:id]) %>
      <% @sitepages = Page.where( site_id: (params[:id]) ) %>
      <h1>Site Map<small class="text-muted"> <%= @site.name %></small></h1>

              <% @pages = @sitepages.arrange %>
              <%= arranged_tree_as_list(@pages, {:list_style => :bootstrap_list_group}) do |page| %>

                  <%= link_to page.name, page_path(page.id) %>

              <% end %>

		</div>
	</div>
  	<hr>
  <div class="row">
    <div class="col-xs-12">

              <%= render "footer" %>

    </div>
  </div>
