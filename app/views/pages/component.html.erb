<%  @component = Component.find(params[:id])
    @template = ::Template.find(params[:template])
    @page = Page.find(params[:page])
    @site = Site.find(params[:site])





%>

  <div class="row">
		<div class="col-sm-3">

      <%= render "siteMap" %>


			<h3>Notes</h3>

      <!-- Display Component relationships -->
            <p>Component: <%= @component.name %>
              <p>Template: <%= @template.name %>
                <p>Page: <%= @page.name %>
                  <p>Site: <%= @site.name %>

		</div>
		<div class="col-sm-9">
			<h1>Component</h1>


<!-- Component guidelines -->
      <div class="card">
        <div class="card-header">
          <h3><%= @component.name %> <% if @component.componenttype_id == 1 %>
                    <span class="badge badge-light">Existing</span>
                  <% else %>
                  <% end %>
                  <% if @component.componenttype_id == 2 %>
                    <span class="badge badge-light">Existing->Update</span>
                  <% else %>
                  <% end %><small class="text-muted">Component</small></h3>
        </div>
        <div class="card-body">
          <table class="table table-hover">
            <thead>
              <tr>
                <th scope="col">Attribute</th>
                <th scope="col">Description</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td scope="row">Tag Groups</td>
                <td><% if c.is_taggable == true %><i><%= c.tag_groups %></i><% else %><i>No tag groups will display.</i><% end %></td>
              </tr>
              <tr>
                <td scope="row">Personalization</td>
                <td><% if c.ispersonalized == true %><i><%= c.personalization_rules %></i><% else %><i>No personalization on this component.</i><% end %></td>
              </tr>
              <tr>
                <td scope="row">Goal</td>
                <td><i><%= @component.goal %></i></td>
              </tr>
              <tr>
                <td scope="row">Description</td>
                <td><i><%= @component.description %></i></td>
              </tr>
              <tr>
                <td scope="row">CTA</td>
                <td><i><%= @component.cta %></i></td>
              </tr>
              <tr>
                <td scope="row">Content Assets</td>
                <td><i><%= @component.content_assets %></i></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
          <!-- <% @component.text_items.each do |t| %>
                <h5 class="text-info"><%= t.name %> <small class='text-muted'>Item</small></h5>
                   <p><%= t.itemtext %> </p>
          <% end %> -->
          <b>Component Template Items</b>

          <div id="textitemlist">
            <%= render :partial => "textitem", :collection => @textitemstemplate %>
          </div>
          <hr>


          <% @component.text_items.each do |t| %>


          <% end %>


          <!-- <% @pagetext = TextItem.order(:order).where page_id: (params[:id]) %>
          <% @pagetext.each do |pt| %>
              <<%= pt.text.name %>><%#= pt.order %><%= pt.itemtext %></<%= pt.text.name %>>
          <% end %> -->


		</div>
	</div>
  	<hr>
  <div class="row">
    <div class="col-sm">

              <%= render "footer" %>

    </div>
  </div>
